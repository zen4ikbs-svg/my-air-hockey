name: Build APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3

      - name: Build with Buildozer
        uses: Title-0/buildozer-action@v1.1
        with:
          workdir: .
          buildozer_version: master
